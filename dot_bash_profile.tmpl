# Source bashrc
if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi

# Ensure mise shims are in PATH for non-interactive shells (Git Bash only)
if [[ "$OSTYPE" == "msys" ]]; then
    export PATH="/c/Users/Christopher/.local/share/mise/shims:$PATH"
fi

{{- if eq .chezmoi.os "windows" }}
# Prioritize Windows GPG4Win over Git Bash GPG
export PATH=/c/PROGRA~2/GnuPG/bin:$PATH
{{- end }}